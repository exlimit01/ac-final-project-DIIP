<!DOCTYPE html>
<html>
  <head>
    <title>DIIP</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <meta name="viewport" content="width=device-width; initial-scale=1">
    <%= favicon_link_tag "favicon.ico", type: 'image/png'%>
  </head>

  <body>
    <div class="wrapper">
      <!--=== Header ===-->
      <div class="header">
        <div class="container">
          <!-- Logo -->
          <a class="logo" href="#">
            <%= image_tag ("diip-logo.png"), style: "max-width: 70px;" %>
          </a>
          <!-- End Logo -->

          <!-- Dropdown -->
            <div class="btn-group pull-right">
              <button type="button" class="btn-u">Profile</button>
              <button type="button" class="btn-u btn-u-split dropdown-toggle" data-toggle="dropdown">
                <i class="fa fa-user"></i>
                <span class="sr-only">Toggle Dropdown</span>
              </button>
              <ul class="dropdown-menu" role="menu">
                <li><a href="#"><i class="fa fa-list"></i> 修改資料</a></li>
                <li class="divider"></li>
                <li><a href="#"><i class="fa fa-cloud-download"></i> 登出</a></li>
              </ul>
            </div>
          <!-- End Dropdown -->

          <!-- Toggle get grouped for better mobile display -->
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="fa fa-bars"></span>
          </button>
          <!-- End Toggle -->
        </div><!--/end container-->

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse mega-menu navbar-responsive-collapse">
          <div class="container">
            <ul class="nav navbar-nav">
              <% if current_user && current_user.profile %>
                <li class="dropdown">
                    <%= link_to "個人資料", empty_profiles_path %>
                </li>
                <li class="dropdown">
                    <%= link_to "任務專區", "#" %>
                </li>
                <li class="dropdown">
                    <%= link_to "搜尋同好", profiles_path %>
                </li>
              <% end %>
            </ul>
          </div><!--/end container-->
        </div><!--/navbar-collapse-->
      </div>
      <!--=== End Header ===-->

  <body>
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <% if flash[:notice].present? %>
            <div class="alert alert-success">
              <%= flash[:notice] %>
            </div>
          <% end %>
          <% if flash[:alert].present? %>
            <div class="alert alert-danger">
              <%= flash[:alert] %>
            </div>
          <% end %>
        </div>
        <div class="col-md-12">
          <% if current_user %>
            <%= "Hi, #{current_user.email}" %>
            <%= link_to "登出", destroy_user_session_path, :method => :delete %>
            <%= link_to "修改資料", edit_registration_path(:user) %>
            <% end %>
        </div>
      </div>

      <section>
        <% if current_user && current_user.profile %>
          <div><%= link_to "個人資料", empty_profiles_path %></div>
          <div><%= link_to "任務", interactions_list_path %></div>
          <div><%= link_to "使用者列表", profiles_path %></div>
        <% end %>
      </section>

      <%= yield %>
    </div>
  </body>
</html>
