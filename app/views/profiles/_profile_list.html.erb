<table class="table">
  <thead>
    <tr>
      <th>暱稱</th>
      <th></th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @profiles.each do |profile| %>
      <tr>
        <% unless profile.is_current_user?(current_user) %>
          <td><%= link_to profile.nickname, profile_path(profile) %></td>
          <%= make_friend_signs(profile) %>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>